/**
 * Shared configuration values for the AppliStash extension
 */

// API Base URL - Change this single location when needed
export const API_BASE_URL = "http://localhost:3000";

console.log(
  "AppliStash: Using API Base URL:",
  API_BASE_URL
);
